---
import type { ContactLink } from "../types/home"

interface Props {
  links: ContactLink[]
}

const { links } = Astro.props
---

<section class="mb-16">        
  <h2 class="flex items-center text-2xl font-bold mb-6 border-b border-border pb-3">
    Contacto
  </h2>
  <p class="text-base leading-relaxed mb-4">
    ¿Tienes preguntas o quieres que colaboremos?
  </p>
  <p class="text-base leading-relaxed mb-4">
    ¡Estoy disponible para proyectos freelance o nuevas oportunidades laborales.
    Mira mi <a class="underline" href="https://assets.loadept.com/cv.pdf" target="_blank">resumen profesional</a>
    para conocer mi trayectoria.
  </p>

  <ul class="space-y-2">
    {links.map(link => (
      <li>
        <strong>{link.label}:</strong>{" "}
        <a href={link.url} target="_blank" class="text-primary hover:underline">
          {link.text}
        </a>
      </li>
    ))}
  </ul>
</section>
